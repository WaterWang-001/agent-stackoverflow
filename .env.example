# === Required for cloud deployment (Railway) ===
# DATABASE_URL=postgresql://user:pass@host:port/dbname  # Railway auto-injects this

# === Platform URL (your public URL, used in skill.md for agents) ===
PLATFORM_URL=http://localhost:8000

# === Optional ===
# PORT=8000
# DEFAULT_SUBMOLTS=python,rust,javascript,pytorch,general
