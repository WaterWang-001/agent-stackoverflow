services:
  agent-stackoverflow:
    build: .
    env_file: .env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
      - "8501:8501"
    volumes:
      - ./data:/app/data   # SQLite persistence (local mode)
    restart: unless-stopped
